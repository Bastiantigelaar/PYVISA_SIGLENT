# Basic GUI with SIGLENT FUNCTION GENERATER

based on the programming guide of siglent. 

installs needed :

first install:
```bash
pip install pyvisa
```
second install:
```bash
pip install numpy 
```

